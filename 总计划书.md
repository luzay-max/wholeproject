# 总计划书 (Master Plan)

本文档为项目优化的执行路线图。每完成一项任务，请在方框内打钩 `[x]`。
**原则**: 每次行动前检查 `约束文档.md`，遇到问题更新 `错误文档.md`。

## 第一阶段：后端基础重构 (Backend Infrastructure)

### 1.1 依赖与配置
- [x] **引入 Redis 依赖**: 在 `pom.xml` 中添加 spring-boot-starter-data-redis。
- [x] **配置 Redis**: 在 `application.yml` 中添加 Redis 连接配置。
- [x] **配置 MyBatis-Plus 逻辑删除**: 在 `application.yml` 中配置 `logic-delete-value`。
- [x] **代码生成/补充**:
    - [x] 生成 `StudentWhitelist` (白名单) 的 Entity, Mapper, Service。
    - [x] 生成 `OperationLog` (操作日志) 的 Entity, Mapper, Service。

### 1.2 实体类更新 (Entity Update)
- [x] **更新 User 实体**: 添加 `accountName`, `studentId`, `college`, `status`, `isDeleted`。
- [x] **更新 LostInfo 实体**: 添加 `lostTime`, `contactInfo`, `isDeleted`。
- [x] **更新 FindInfo 实体**: 添加 `foundTime`, `contactInfo`, `isDeleted`。
- [x] **添加逻辑删除注解**: 在上述实体类的 `isDeleted` 字段上添加 `@TableLogic`。

## 第二阶段：用户模块深度优化 (User Module)

### 2.1 注册逻辑重构
- [x] **白名单导入接口**: 实现 Excel 导入 `StudentWhitelist` 功能 (Admin)。
- [x] **注册校验升级**: 注册时校验 `studentId` + `name` 是否在白名单中。
- [x] **密码加密**: 注册与重置密码统一使用 BCrypt，加固旧 MD5 兼容校验。
- [x] **用户修改密码**: 实现用户修改密码功能（满足旧密码校验+新密码规则）。
- [x] **字段扩充**: 增加 `college` (学院), `accountName` (账户名), `status` (状态: 正常/封禁)。

### 2.2 登录体系升级
- [x] **登录逻辑调整**: 支持 `accountName` 登录。
- [x] **Remember Me 后端实现**:
    - [x] 登录接口新增 `rememberMe` 参数。
    - [x] 生成 Refresh Token 并存入 Redis (7天过期)。
    - [x] 实现 Refresh Token 刷新 Access Token 的接口。

## 第三阶段：核心业务逻辑升级 (Core Business)

### 3.1 失物招领 CRUD 增强
- [x] **发布接口**:
    - [x] 接收 `lostTime`/`foundTime` 并校验 (不能是未来时间)。
    - [x] 接收 `contactInfo` (联系人/联系方式)。
    - [x] 默认状态设为 `PENDING` (待审核)。
- [x] **逻辑删除接口**: 实现 `DELETE` 接口，仅更新 `is_deleted` 字段 (数据留痕)。
- [x] **查询接口**: 
    - [x] 普通用户列表仅显示 `APPROVED` 状态的数据。
    - [x] 个人中心可查看 `PENDING`/`APPROVED`/`REJECTED` 数据。
    - [x] 后端分页机制 (避免全量加载)。

### 3.2 审核与管理 (Admin)
- [x] **管理列表 API**: 支持按状态 (`PENDING`, `APPROVED`, `HIDDEN`, `TRASH`) 筛选。
- [x] **审核动作 API**:
    - [x] `approve`: 状态 -> `APPROVED`
    - [x] `reject`: 状态 -> `REJECTED` (需填写理由)
    - [x] `hide`: 状态 -> `HIDDEN` (下架/违规处理)
    - [x] `recover`: 状态 -> `PENDING` (从回收站恢复)
- [x] **数据导出**: 为学校表彰提供数据导出支持 (Excel导出)。
- [x] **全量 CRUD 补全**:
    - [x] **失物/招领管理**: 增加新增、编辑接口 (AdminController)。
    - [x] **评论管理**: 增加新增、编辑、删除接口 (AdminController)。
    - [x] **活动日志管理**: 增加新增、编辑、删除接口 (AdminController)。
    - [x] **用户管理**: 增加新增、编辑接口 (AdminController)。
    - [x] **光荣榜管理**: 增加周期编辑、榜单成员增删改接口 (AdminController)。

### 3.3 日志切面
- [x] **实现 @Log 注解**: 用于标记关键方法 (增删改、登录)。
- [x] **实现 AOP 切面**: 拦截 @Log，异步记录操作日志到数据库。

## 第四阶段：前端重构与对接 (Frontend Refactor)

### 4.1 基础组件与工具
- [x] **封装 Axios**: 处理 Token 自动刷新 (Refresh Token) 及 请求队列。
- [x] **封装指令**: `v-focus-next` (回车跳焦，提升表单填写效率)。
- [x] **通用校验**: 封装手机号、邮箱、身份证/学号、密码复杂度正则校验函数。
- [x] **表单体验**:
    - [x] 输入框回车跳转。
    - [x] 必填项与格式校验实时反馈。

### 4.2 页面改造 (按优先级)
- [x] **注册/登录页**: 增加“记住我”勾选，适配新注册逻辑。
- [x] **发布页**: 新增时间选择器 (限制范围: 当前时间之前)、联系人输入框。
- [x] **管理员后台**:
    - [x] 重构为多标签页 (Tabs) 布局 (支持多任务并行)。
    - [x] 实现白名单导入页面 (Excel上传)。
    - [x] **白名单功能增强**:
        - [x] 手动添加单条白名单。
        - [x] 下载 Excel 导入模板。
    - [x] **用户管理增强**:
        - [x] 实现封禁/解封功能 (状态管理)。
        - [x] UI 风格统一化 (适配新设计)。
        - [x] 增加新增用户、编辑用户弹窗 (UserManagement.vue)。
    - [x] **光荣榜管理增强**:
        - [x] 增加光荣榜周期编辑功能。
        - [x] 增加榜单成员增删改功能 (HonorManagement.vue)。
    - [x] 实现统一的“信息管理中心”页面 (包含审核流: 待审/通过/驳回/删除)。
    - [x] 增加面包屑导航 (优化路径感知)。
- [x] **公共列表页**: 增加面包屑导航，优化 UI 卡片样式 (16:9图片，状态标签，悬停效果)。
- [x] **个人中心**:
    - [x] 修改密码页面。
    - [x] 个人发布记录管理 (查看状态)。

## 第五阶段：验收与文档 (Acceptance)
- [x] **功能测试**: 验证所有 Checkbox 功能 (代码级验证已完成)。
- [x] **更新文档**: 根据实际开发情况更新 `API文档.md` 和 `skill.md`。
- [x] **最终Review**: 检查 `错误文档.md` 是否已归档所有坑点。

## 第六阶段：缺陷修复与优化 (Bug Fixes & Optimization)
- [x] **分页组件优化**: 修复分页跳转输入框 (`jumper`) 不显示的问题 (可能是样式或容器宽度导致)。
- [x] **活动日志报错修复**: 修复 `ActivityManagement.vue` 页面 `ResizeObserver` 报错 (`useSelect.ts`) 问题。

## 第七阶段：需求补充与完善 (Requirements Supplement)
- [x] **白名单功能补全**:
    - [x] **白名单导入接口**: 实现 Excel 导入白名单功能 (Admin)。
    - [x] **白名单模板下载**: 实现白名单导入模板下载功能。
    - [x] **前端对接**: 在 `WhitelistPage.vue` 中增加导入和下载模板按钮及逻辑。
- [x] **细节完善**:
    - [x] **用户昵称字段**: 确认使用 `accountName` 代替 `nickname`，并在用户中心展示。
    - [x] **姓名校验优化**: 优化姓名校验正则 (允许生僻字，禁止乱码)。

## 第八阶段：数据字典模块 (Data Dictionary)

### 8.1 基础架构
- [x] **数据库设计**:
    - [x] 设计 `sys_dict_type` 表 (字典类型: id, name, type, status, remark).
    - [x] 设计 `sys_dict_data` 表 (字典数据: id, type, label, value, sort, status).
- [x] **后端开发**:
    - [x] 生成 Entity, Mapper, Service, Controller.
    - [x] 实现字典类型与数据的 CRUD 接口.
    - [x] 实现 `getDictByType` 公共接口 (供前端下拉框使用).
    - [ ] (可选) 集成 Redis 缓存字典数据.

### 8.2 前端管理
- [x] **管理页面**:
    - [x] 新增 "系统管理/数据字典" 页面.
    - [x] 实现字典类型的增删改查.
    - [x] 实现字典数据的配置 (列表管理, 排序, 状态控制).
- [x] **前端组件**:
    - [x] 封装 `DictSelect` 组件 (自动加载字典数据).
    - [x] 封装 `DictTag` 组件 (用于列表页回显 Label).

### 8.3 业务迁移
- [x] **物品类型迁移**: 将 `ItemType` 枚举迁移至数据字典 (类型: `item_type`).
- [x] **表单改造**: 失物/招领发布页、搜索栏使用数据字典接口替代硬编码选项.

## 第九阶段：移动端兼容改造 (Mobile Web Compatibility)
- [ ] **目标约束**：在手机网页上保证可用性与可读性，且不丢失任何原有功能（发布、搜索、筛选、分页、详情、管理入口、个人中心等）。

### 9.1 布局与导航适配
- [x] **响应式断点梳理**：统一 <=1200 / <=992 / <=768 / <=480 的布局策略，避免页面间断点行为不一致。
- [x] **顶部导航适配**：移动端收敛导航与下拉菜单交互，确保管理员菜单与用户菜单可完整操作。
- [x] **管理端框架适配**：侧边栏、标签页、面包屑在小屏下可折叠/横向滚动，避免遮挡主内容区。

### 9.2 列表与表单可用性
- [x] **列表页适配**：失物/招领/评论/日志/字典等列表在移动端支持横向滚动或卡片化降级，保证关键列可访问。
- [x] **分页组件统一**：移动端分页保留 sizes + jumper 的可用性，必要时按断点做紧凑展示但不移除能力。
- [x] **发布/编辑表单适配**：表单项单列化、按钮全宽化、日期选择器与上传组件触控优化，保证提交流程完整。

### 9.3 交互与功能回归
- [x] **交互一致性检查**：确认移动端不引入“输入即搜索误触发”、弹窗遮挡、按钮不可点击等问题。
- [x] **功能回归清单**：逐页验证增删改查、审核流、复制按钮、图片预览、详情弹窗、路由跳转。
- [x] **权限与路由回归**：验证管理员/普通用户在移动端的权限边界与菜单可达性。

### 9.4 验收标准
- [ ] **视觉验收**：主流手机宽度下无明显溢出、错位、遮挡、乱码。
- [ ] **功能验收**：与桌面端功能对齐，无功能缺失或降级为不可用。
- [ ] **性能验收**：移动端首屏与交互响应可接受，无明显卡顿。

## 第十阶段：前后端联调缺口（2026-02-24）

### 10.1 高优先级（前端已调用，后端未实现/协议不一致）
- [x] **补齐批量删除失物接口**：新增 `DELETE /admin/lost/batch`（前端 `batchDeleteLostItems` 在 `InfoCenter/LostManagement` 已调用）。
- [x] **补齐批量删除招领接口**：新增 `DELETE /admin/find/batch`（前端 `batchDeleteFindItems` 在 `InfoCenter/FindManagement` 已调用）。
- [x] **统一白名单批量删除请求体**：前端 `batchDeleteWhitelist` 当前发送 `{ ids }`，后端 `batchDeleteWhitelist(@RequestBody List<Long> ids)` 需统一为同一协议。
- [x] **统一分页参数命名**：`InfoCenter.vue` 当前发送 `size`，后端接口使用 `pageSize`，需统一避免分页大小失效。

### 10.2 中优先级（预留接口未落地）
- [x] **招领搜索接口二选一**：实现 `GET /find/search`，或删除前端 `searchFind` 并统一走 `/find/list`。
- [x] **审核统计接口二选一**：实现 `GET /audit/history`、`GET /audit/stats`、`GET /admin/recentItems`，或移除 `auditApi.js` 中未使用定义。
- [x] **留言通知接口二选一**：实现 `/chat/*` 接口组（`leaveMessage/replyMessage/messageList/deleteMessage/notifications/markAsRead`），或下线 `chatApi.js`。
- [x] **邮件相关接口二选一**：实现 `/auth/sendCode`、`/auth/verifyEmail`、`/auth/update`，或清理 `authApi.js` 中未启用定义。

### 10.3 数据字典联调与脏数据治理
- [x] **统一活动动作字典值大小写**：`activity_action` 与 `activities.action` 建议统一为大写（`PUBLISH/UPDATE_STATUS/COMMENT/HIDE/RECOVER`）。
- [x] **执行字典补齐脚本**：使用 `data_cleanup_script.sql` 中带 `utf8mb4_unicode_ci` 的补齐 SQL，避免 `1267 Illegal mix of collations`。
- [x] **修复活动日志空白回显**：联调修复“操作类型/物品类型为空”（字典值、大小写、历史脏数据三者对齐）。

### 10.4 管理端页面一致性（你提出的UI一致化）
- [x] **数据字典详情页一致化**：`/admin/dict/data` 与其他管理页统一（标签页行为、返回路径、布局组件）。
- [x] **修复标签关闭回跳路径**：`AdminLayout.vue` 关闭标签默认回跳 `/admin/audit`（路由不存在），改为有效管理页（建议 `/admin/info`）。
