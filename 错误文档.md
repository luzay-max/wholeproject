# 错误日志与经验总结 (Error Log & Lessons Learned)

本文档用于记录开发过程中遇到的典型错误、Bug 及其解决方案，旨在避免重复踩坑。

| 日期 | 问题描述 | 原因分析 | 解决方案 | 记录人 |
| :--- | :--- | :--- | :--- | :--- |
| 2026-02-04 | 初始记录 | - | 建立此文档以追踪后续问题 | System |
| 2026-02-04 | FindInfo 缺少访问器导致 setContactInfo/setFoundTime 编译错误 | 实体未同步新增字段；控制器已使用新字段 | 为 FindInfo 添加相应 getter/setter 方法 | Trae |
| 2026-02-04 | LostInfo 缺少访问器导致 setContactInfo/setLostTime 编译错误 | 同步问题：实体未提供访问方法 | 为 LostInfo 添加相应 getter/setter 方法 | Trae |
| 2026-02-04 | Maven 编译报 NoSuchFieldError: JCTree$JCImport.qualid | JDK 与编译插件/注解处理器版本不匹配 | 升级 Lombok 至 1.18.34；maven-compiler-plugin 设置 release=17 并配置注解处理器 | Trae |
| 2026-02-04 | application.yaml 与 application.yml 冲突 | 配置文件重复导致加载不一致 | 合并内容并仅保留 application.yaml | Trae |
| 2026-02-04 | 切面类无法编译（org.aspectj.* 不存在） | 缺少 AOP 依赖 | 在 pom.xml 引入 spring-boot-starter-aop | Trae |
| 2026-02-04 | 操作日志未入库 | 实体与表结构不一致：实体含 ip、userId 为 String；表无 ip 且 user_id 为 bigint；切面吞异常 | 对齐实体为 (id,userId:Long,actionType,content,createTime)；将 ip 写入 content JSON；切面打印异常便于排查 | Trae |
| 2026-02-24 | 字典补齐 SQL 报错 `1267 Illegal mix of collations` | `operation_log.action_type` 与 `sys_dict_data.dict_value` 使用不同排序规则，直接 `=` 比较触发冲突 | 在补齐 SQL 中统一使用 `CONVERT(... USING utf8mb4) COLLATE utf8mb4_unicode_ci` 再比较，脚本可稳定执行 | Codex |
| 2026-02-24 | 管理员刷新 ` /admin/* ` 被误判无权限并跳首页 | 路由守卫先判断 `isAdmin`，但刷新时角色尚未从接口回填，`role` 为空导致误拦截 | 在路由守卫中对受限路由先执行用户信息预热（有 token 且 role 为空时调用 `getUserInfo`），再做权限判断 | Codex |

## 典型案例模板

### [Bug-001] 简短描述
*   **现象**: 
*   **原因**: 
*   **解决**: 
*   **教训**: 

---
*注：每次解决复杂 Bug 后请更新此文档。*

## 经验教训总结
- 接口唯一性：同一控制器中不得重复定义相同路径与方法的映射，避免重复 @DeleteMapping("/{id}") 导致方法冲突。
- 实体与控制器同步：在 Controller/DTO 使用新字段前，实体必须同步新增并提供对应的 getter/setter。
- 状态与时间规范：状态值统一大写存储（PENDING/APPROVED/REJECTED/SOLVED）；业务时间不得晚于当前时间，使用 Asia/Shanghai 时区统一校验。
- 逻辑删除优先：删除接口统一使用逻辑删除（@TableLogic），普通列表不展示已删除数据。
- 构建与依赖：Maven compiler-plugin 使用 release=17，Lombok 1.18.34 作为注解处理器，避免 JDK/插件不匹配的编译错误。
- 配置文件统一：统一使用 application.yaml，禁止同时存在 application.yml 以免加载冲突。
- AOP 使用约束：使用切面需引入 spring-boot-starter-aop；切面不得吞异常（至少记录 error），避免静默失败。
- 操作日志规范：日志实体与表结构必须一致；敏感字段（password/captcha）不得写入日志；允许异步写库但失败需留痕。
